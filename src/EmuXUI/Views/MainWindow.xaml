<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="EmuXUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EmuXUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:EmuXUI.ViewModels"
    xmlns:codeEditor="using:TextControlBoxNS"
    mc:Ignorable="d"
    Title="EmuX">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height = "Auto" />
            <RowDefinition Height = "Auto" />
            <RowDefinition Height = "*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name = "TitleBarGrid" Grid.Row = "0" Grid.Column = "0" Background = "#80000000">
            <Grid.RowDefinitions>
                <RowDefinition Height = "*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width = "*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Row = "0" Grid.Column = "0" Margin = "10,10,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height = "*" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width = "*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row = "0" Grid.Column = "0" HorizontalAlignment = "Center" FontSize = "16" Text = "EmuX" />
            </Grid>
        </Grid>
        
        <MenuBar Grid.Row = "1" Grid.Column = "0" Background = "#50000000">
            <MenuBarItem Title = "File">
                <MenuBarItem.Items>
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemOpenFile" Text = "Open file" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemSaveFile" Text = "Save file" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemSaveFileAs" Text = "Save file as" />
                </MenuBarItem.Items>
            </MenuBarItem>

            <MenuBarItem Title = "Edit">
                <MenuBarItem.Items>
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemUndo" Text = "Undo" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemRedo" Text = "Redo" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemFind" Text = "Find" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemFindAll" Text = "Find all" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemReplace" Text = "Replace" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemReplaceAll" Text = "Replace all" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemFontSettings" Text = "Font settings" />
                    <MenuFlyoutItem x:Name = "MenuFlyoutItemSyntaxSettings" Text = "Sytntax settings" />
                </MenuBarItem.Items>
            </MenuBarItem>

            <MenuBarItem x:Name = "VirtualMachineMenuBarItem" Title = "Virtual Machine" />
            <MenuBarItem x:Name = "ExecuteMenuBarItem" Title = "Execute" />
            <MenuBarItem x:Name = "AboutMenuBarItem" Title = "About" />
        </MenuBar>
        
        <Grid Grid.Row = "2" Grid.Column = "0">
            <Grid.RowDefinitions>
                <RowDefinition Height = "*" />
                <RowDefinition Height = "Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width = "*" />
            </Grid.ColumnDefinitions>

            <codeEditor:TextControlBox x:Name = "SourceCodeTextControlBox" Grid.Row = "0" Grid.Column = "0" Grid.ColumnSpan = "2" KeyDown = "SourceCodeTextControlBox_KeyDown" PointerPressed = "SourceCodeTextControlBox_PointerPressed" />

            <Grid Grid.Row = "1" Grid.Column = "0" Background = "#50000000">
                <Grid.RowDefinitions>
                    <RowDefinition Height = "*" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width = "*" />
                    <ColumnDefinition Width = "*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row = "0" Grid.Column = "0" Orientation = "Horizontal" Margin = "10,10,10,10" Opacity = "0.75">
                    <TextBlock x:Name = "SizeTextBlock" Text = "Size: 0" />
                </StackPanel>

                <StackPanel Grid.Row = "0" Grid.Column = "1" Orientation = "Horizontal" FlowDirection = "RightToLeft" Margin = "10,10,10,10" Spacing = "10" Opacity = "0.75">
                    <TextBlock x:Name = "RowTextBlock" Text = "Row: 0" />
                    <TextBlock x:Name = "ColumnTextBlock" Text = "Col: 0" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
