<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="EmuXUI.Popups.InfoPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EmuXUI.Popups"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="BlankWindow1">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name = "mainGrid" MaxHeight = "400" MaxWidth = "400" Background = "{StaticResource Grey900}">
        <Grid.RowDefinitions>
            <RowDefinition Height = "Auto" />
            <RowDefinition Height = "*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name = "TitleBarGrid" Grid.Row = "0" Grid.Column = "0" Background = "{StaticResource Grey950}">
            <Grid.RowDefinitions>
                <RowDefinition Height = "*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width = "*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Row = "0" Grid.Column = "0" Margin = "10,10,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height = "*" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width = "*" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name = "textBlockInfoTitle" Grid.Row = "0" Grid.Column = "0" HorizontalAlignment = "Center" FontSize = "16" Text = "EmuX" />
            </Grid>
        </Grid>

        <Grid Grid.Row = "1" Grid.Column = "0" Padding = "10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height = "*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width = "*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Row = "1" Grid.Column = "0" Padding = "10,10,10,10" CornerRadius = "10" Background = "{StaticResource Grey925}">
                <TextBlock x:Name = "textBlockInfoContents" TextWrapping = "WrapWholeWords" />
            </ScrollViewer>
        </Grid>
    </Grid>
    
</Window>
